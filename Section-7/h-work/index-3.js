function setup() {
    var name = "Benjamin";
}
console.log(name);
//3-2. A varibale declared by var will not exist after the function finishes running;
function setup() {
    let name = "Benjamin";
}
console.log(name);
//3-1. A varibale declared by let will not exist after the function finishes running;