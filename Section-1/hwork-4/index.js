let x = alert('Coding might not be easy, but still fun');
 console.log('Coding might not be easy, but still fun');
let studentcount = 16;
console.log(studentcount);
console.log('coding might not be easy, but still fun');
console.log(studentcount+1);